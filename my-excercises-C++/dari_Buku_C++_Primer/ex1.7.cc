int main() {

    /** ini di luar 
    * /* ini di dalam (nested) */
    /* ini komen di luar lagi  // but if aded  it is like this 
    */

    // it is like this /* put any thing inside this and close with this */
    // example 

    /* /* */ */ /* */
}