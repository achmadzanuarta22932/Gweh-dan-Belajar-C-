#include <iostream> 
#include <vector>
using namespace std;

int main() {
    vector<int> nilai;

    nilai.push_back(80);
    nilai.push_back(85);
    nilai.push_back(90);

    cout<<nilai.size();

    return 0;
}